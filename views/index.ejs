
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form px-10">
            <form action="/create" method="post"> <input
                    class="block w-full px-2 rounded-md outline-none mb-4 bg-zinc-500" type="text" name="title"
                    placeholder="Title goes here" /> <textarea
                    class="block w-full px-5 rounded-md outline-none resize-none bg-zinc-500" name="detail"
                    placeholder="Description about it"></textarea> <input class="mt-2 bg-blue-600 px-4 py-1 rounded-md"
                    type="submit" value="Create Task"> </form>
        </div>
        <div class="tasks flex gap-3 flex-wrap p-10">
            <% if (files.length> 0) { %> <% files.forEach(function(val) { %>
                    <div class="text-blue-400 inline-block mt-3 task w-72 px-3 py-3 rounded-md bg-zinc-500">
                        <h1 class="text-white text-3xl tracking-tighter">
                            <%= val %></h1>
                        </h1> <a class="text-blue-400 inline-block mt-3" href="/files/<%= val %>">Read More..</a>
                    </div>
                    <% }) %>
                        <% } else { %>
                            <h3 class="text-zinc-600">No Tasks Yet</h3>
                            <% } %>
        </div>
    </div>
</body>

</html>